using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// セーブデータロード用のボタン
public class LoadButton : ButtonAction
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    public override void Action()
	{
        Debug.Log("ロードします");
	}

    // Update is called once per frame
    void Update()
    {
        
    }
}
